
## ✅ المميزات
- نموذج متعدد الحقول مع تحقق من البيانات
- شاشة تسجيل دخول مبسطة
- يدعم أنظمة Android / iOS / Web / Desktop

## 🔧 المتطلبات
- Flutter (تأكد من تثبيت إصدار مستقر)
- Dart SDK
- جهاز أو محاكي لتشغيل التطبيق

## 🚀 تشغيل المشروع
1. استرجع التبعيات:
   ```bash
   flutter pub get
   ```
2. شغّل التطبيق:
   ```bash
   flutter run
   ```
3. لتشغيل على جهاز محدد:
   ```bash
   flutter run -d <device-id>
   ```

## 🗂 بنية أساسية للمجلدات
- `lib/main.dart` — نقطة الدخول
- `lib/complete_form_screen.dart` — شاشة النموذج
- `lib/login_screen.dart` — شاشة تسجيل الدخول

## ✅ اختبارات
- شغّل الاختبارات الوحدوية:
  ```bash
  flutter test
  ```
